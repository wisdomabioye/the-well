# Implementation task list

The canonical completed, active, pending, and blocked task ledger is
[`docs/implementation/task-ledger.md`](../docs/implementation/task-ledger.md).

This compatibility pointer remains where the task runner historically looked for its queue. Update
only the canonical ledger as implementation progresses; task-specific evidence remains in this
`references/` directory.
