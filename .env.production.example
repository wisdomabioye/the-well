# Runtime environment; independent from the hosting provider.
APP_ENV=production
# Canonical origin used for links and redirects. Replace before deployment.
PUBLIC_BASE_URL=https://example.invalid
# Product name safe to expose in browser bundles.
NEXT_PUBLIC_APP_NAME=Adorbitals
# Production PostgreSQL runtime URL. Replace with a deployment secret.
DATABASE_URL=postgresql://user:password@database.example.invalid/app
# Optional privileged direct migration URL. Replace with a deployment secret or leave blank.
DATABASE_MIGRATION_URL=
# Production PostgreSQL TLS policy.
DATABASE_SSL_MODE=verify-full
# Maximum runtime connections per production process.
DATABASE_POOL_MAX=10
# Maximum milliseconds to acquire a PostgreSQL connection.
DATABASE_ACQUIRE_TIMEOUT_MS=5000
# Milliseconds before an idle pooled connection is closed.
DATABASE_IDLE_TIMEOUT_MS=30000
# Maximum PostgreSQL statement execution time in milliseconds.
DATABASE_STATEMENT_TIMEOUT_MS=15000
